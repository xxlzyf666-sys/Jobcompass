# 验收记录

## 个人收款与次数版本（2026-09-13）

环境：Linux amd64、Go 1.27.1、Chrome 151。独立本地数据库，二维码明确标注 TEST ONLY / NOT A PAYMENT CODE；使用虚构材料和固定模型替身，没有进行真实收款、退款或模型调用。

- 27 组 Go 测试通过（原有 17 组 + 账务与账号 10 组），`go vet`、三份前端模块的语法检查和 `git diff --check` 通过。
- 账号注册、用户名归一化、密码及恢复码派生保存、会话轮换、恢复后注销旧会话、旧恢复码不可复用、匿名材料加入账号及不同注册账号隔离有测试。报告恢复码不能获得账号或余额。
- 下单政策确认、服务端定价、配置版本冲突、请求重放、旧价格与收款码快照、私有订单访问有测试。申报不发放次数；错误金额被拒绝，同笔到账并发确认只发放一次，跨订单复用交易单号被拒绝。
- 测试两个请求争用最后一次额度、队列满时回滚、重复材料提交、成功扣次、自动重试、终止失败返还、旧 worker 迟到结果、删除和到期清理。财务记录不随材料到期删除。
- 精修追问和一次改写共用一轮额度，终止失败返还、手动重试重新预占，成功后不能重复改写。付费面试拒绝 8 题，5 题和全部反馈只使用 1 场；报告共享者不能使用原账号的付费轮次。免费轮次继续完成时不扣费，关闭购买不破坏已开启付费任务的失败返还。
- 退款申请暂停剩余次数，管理员受理后不能自行撤回；关联任务在运行时不能登记退款。部分 / 多笔退款、金额上限、单号去重、收回所有未用次数、阻止退款后继续使用原轮次有测试。
- 管理员会话、独立 CSRF、密钥轮换、图片格式与大小限制有测试。v2 到 v3 升级保留旧报告、准备资料和会话；过期订单的实际付款仍可申报。
- 浏览器走通真实表单交互：配置收款码和套餐、注册、同意规则下单、申报交易单号、后台确认到账；随后使用 1 次诊断、1 轮精修（追问和改写）、1 次岗位适配和 1 场三题面试。数据库确认四种各计 1 次，面试逐题反馈不重复扣次。
- 最终构建重启后账号、订单和余额仍在。退出后生成提示登录，登录成功返回诊断页，已填写的 376 字符简历与 253 字符 JD 保留。
- 浏览器完成用户退款申请、管理员受理和模拟部分退款登记，累计退款 ¥5.00，全部未用次数收回，已用次数保留。
- 320 / 390 像素宽度无横向溢出，手机端先展示金额、收款名称和二维码，再填写付款确认。未发现页面脚本异常，首页来源提示保持隐藏。

截图在忽略目录 `artifacts/billing/`。普通测试中的并发用例已通过；额外尝试 `go test -race` 时环境缺少 C 编译器，未完成 race 检测。构建临时目录曾达到 `/tmp` 配额，迁移项目构建缓存到忽略目录后完成最终测试与构建。以上模拟核款不证明真实支付宝 / 微信到账、退款或模型质量，真实购买默认关闭，需运营者配置并实际核对收款资料后开启。

## 求职准备版本（2026-09-13）

环境：Linux amd64、Go 1.27.1、Chrome 151。使用虚构简历、固定本地 Anthropic 协议替身，不发送真实模型请求。

- 17 组 Go 测试通过（首版 9 组 + 新增 8 组，另含来源核验子案例）；`go vet -buildvcs=false ./...` 与前端 JavaScript 语法检查通过。
- 自动化覆盖追问 → 保存确认事实 → 生成建议 → 逐条采纳；建议生成不会自动改写正文，旧 revision 或重复回答返回冲突。
- 多个岗位版本独立保存，事实可更正和删除；更正使旧建议失效。导出权限隔离，HTML 正确转义，仅输出选定的已保存正文。
- 面试任务携带真实的前一轮回答，按选定题数结束；逐题反馈核对回答引用。简历后续修改不影响面试快照，提前结束只复盘已答题，重新练习会携带原复盘的薄弱点。
- 访问权限、CSRF、生成同意、共享队列、独立 AI 次数限制、并发提交、旧 worker 租约、有限重试、删除及到期后禁止写回均有测试。
- 模拟 v1 数据库升级后，旧诊断材料仍完整，schema version 2 正确建立；恢复码可以恢复全部准备资料。
- 真实浏览器走通补充三项事实、查看改写对比、采纳、复制新 JD 版本、编辑正文及事实、生成岗位修改建议。切换页内步骤保留草稿，刷新后保留所选版本及服务器已保存内容。
- 实际下载简历文字和面试复盘 Markdown；验证打印按钮触发打印，生成简历 PDF 并检查文字完整性与页面渲染。独立打印页没有 JD、恢复码或待采纳建议。
- 三题文字面试逐题追问成功；第二题前刷新可恢复。复盘显示三条回答及反馈；从薄弱点开始第二场练习，回答一题后提前结束，仅复盘这一题。
- 清空 Cookie 后工作台无法访问；通过原恢复码找回两个版本及完整面试。删除报告后工作台返回 404，原恢复码失效。
- 320 px 下精修、版本和面试页面无横向溢出；390 px 面试复盘布局检查通过。没有发现页面脚本异常。
- 新主界面模块与样式使用内容版本号，最终构建重启后旧资料可继续使用。

本次截图、实际导出文件在 `artifacts/preparation/`，仅为本地测试证据，不进入源码提交或部署包。完成少量浏览器联调后一次进程 RSS 采样为 18,676 KiB（约 18.2 MiB）；不含本地模拟服务、浏览器或操作系统，未进行真实上游并发负载测试。

真实模型的改写和追问质量、真人使用反馈、服务器对外部署及收款均不属于上述测试的已验证结果。使用真实模型前按 `evaluation.md` 完成授权样本验收。

## 首版（2026-09-12）

日期：2026-09-12。环境：Linux amd64、Go 1.27.1、Chrome 151。

## 已通过

- 9 组 Go 自动化测试（含引文校验子案例）；`go vet` 通过。
- 原文引用不匹配、伪造引用、无效状态和错误关联会被拒绝；分类计数由服务计算。
- 当前会话查看与导出、其他会话隔离、恢复码授权、删除后撤销访问。
- 明确同意、CSRF、请求长度、队列和次数限制、重复提交。
- 材料加密、错误密钥拒绝、到期清理、任务租约恢复和有限重试。
- 用户删除后，迟到的模型结果不能重新创建报告。
- 中文 PDF 在浏览器内提取，兼容汉字归一化后人工检查再提交。提取阶段 HAR 只记录两次本地脚本 GET，没有 PDF 上传。
- 使用本地模拟 Anthropic 响应完成 PDF → 提交 → 排队 → 六项对照报告的浏览器流程。
- 状态筛选可用；导出时包含全部要求，不受页面筛选影响。
- Markdown 真实下载成功；生成 3 页测试 PDF，检查了文字完整性及末页排版。
- 清除 Cookie 后报告不可直接访问；在原报告页面输入恢复码可恢复；删除后相同恢复码失效。
- 损坏 PDF 有明确错误提示；320 px、390 px 手机宽度无横向溢出；浏览器未报告脚本异常。
- 网页 CSS / JS 带内容版本号，避免程序更新后继续引用旧资源。

截图保存在 `artifacts/`，为本地预览和固定虚构示例，不包含真人简历。

## 资源观测

构建得到约 18 MiB 的 Linux amd64 单二进制，已嵌入网页及 PDF.js。两次本机空闲观测的 RSS 约为 11—14 MiB；这是完成少量本地联调后的采样，不包含 Caddy 和操作系统，也不是高并发峰值或性能承诺。

默认 2 个任务 worker；原 PDF 不进入服务器；目标服务器不用运行 Node、Go 编译器或 PDF 解析进程。

## 尚待实际配置后验证

- 未提供真实 Claude API Key 和可用模型标识，因此未发送真实模型请求。
- 已验证请求协议及模型异常处理，但尚未完成真实模型诊断质量评估。
- 真实服务器的域名、HTTPS、线路与 systemd 部署需要在实际机器上确认；本次未操作远程服务器。
- 用户需求及真人对照评估参见 `evaluation.md`。

本地模拟服务只用于软件流程验证，输出明确标注为非真实 AI。正常启动在缺少模型配置时仅开放示例预览，不接收真实材料。
